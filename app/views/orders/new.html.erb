<%- model_class = Order -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>
<% unless @order_positions.blank? %>
    <%= render :partial => 'form' %>
<% else %>
    Your basket is empty.
<% end %>
